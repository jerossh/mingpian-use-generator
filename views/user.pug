extends layout
block content
  form(method="post", action="/we/get", enctype="application/x-www-form-urlencoded", role="form")
    label 用户
    textarea(rows="3", cols="20", name="article[link]"  autofocus)
    button(type="sublimt") 添加
    button(type="reset") 清空
